# 借还本 MVP — 产品需求

> 文档版本：与当前代码同步（2026-08）  
> 产品英文名：household-IOU-book  
> 适用目录：`mvp/`

---

## 1. 产品概述

### 1.1 背景与定位

「借还本」是一款面向家庭的借还款账本 Web 应用，帮助用户记录与亲戚、朋友、机构之间的资金往来，实时掌握**应收（别人欠我的）**与**应付（我欠别人的）**余额，并通过图表与报告辅助复盘。

本 MVP 为**单用户本地部署版**：每位注册用户拥有独立账本，数据互不共享。

### 1.2 目标用户


| 用户类型    | 典型场景           |
| ------- | -------------- |
| 家庭财务管理者 | 记录亲戚间装修借款、临时周转 |
| 个人记账用户  | 追踪朋友间的小额借还     |
| 有负债的家庭  | 管理房贷、机构贷款等应付项  |


### 1.3 核心价值

- **记清楚**：结构化表单 + 预览确认，减少录入错误
- **算得准**：自动汇总应收/应付，按相关方拆分
- **看得懂**：概览、图表、PDF 报告多维度呈现
- **可迁移**：Excel 导入/导出，便于备份与校对

---

## 2. 核心概念

### 2.1 账目类型（AccountType）


| 枚举值          | 中文  | 含义     |
| ------------ | --- | ------ |
| `RECEIVABLE` | 应收  | 别人欠我的钱 |
| `PAYABLE`    | 应付  | 我欠别人的钱 |




### 2.2 交易类型（TransactionType）


| 枚举值      | 中文  | 对余额的影响   |
| -------- | --- | -------- |
| `BORROW` | 借   | 增加对应账目余额 |
| `REPAY`  | 还   | 减少对应账目余额 |




### 2.3 相关方（Party）

资金往来的对象，分为三类：


| 类型             | 中文    | 示例    |
| -------------- | ----- | ----- |
| `RELATIVE`     | 亲戚    | 张三、王五 |
| `FRIEND`       | 朋友    | 李四    |
| `ORGANIZATION` | 机构/公司 | 某银行   |


同一用户下相关方名称唯一；记一笔时必须从已有相关方中选择。

### 2.4 余额计算规则

- **借（BORROW）**：对应账目余额 **+** 金额
- **还（REPAY）**：对应账目余额 **−** 金额
- 展示时对净余额做下限保护：`max(0, 净余额)`，避免出现负数展示
- 录入预览时：若操作会导致余额为负，系统给出警告

---



## 3. 功能需求



### 3.1 用户认证



#### 3.1.1 注册

- 用户名：2–32 位，仅允许字母、数字、下划线
- 密码：至少 8 位，须同时包含字母、数字、特殊符号
- 须填写确认密码
- **用户名可用性检查**（注册页）：
  - 输入停止 **400ms** 后自动查重（防抖）
  - 用户名输入框**失焦**时立即查重
  - 提示「正在检查…」「✓ 用户名可用」或「✗ 已被占用 / 格式错误」
  - 用户名已被占用时禁用「注册并登录」按钮
- 提交时服务端再次校验用户名是否已被占用（数据库 `@unique` 兜底）
- 注册成功后自动登录并跳转首页



#### 3.1.2 登录

- 用户名 + 密码登录
- 支持 `?next=` 参数，登录成功后跳回原访问页面（须为站内相对路径，防开放重定向）
- 已登录且数据库中用户仍存在时，访问登录/注册页自动跳转首页（页面层 `getOptionalUser` 判断，非 proxy 层）



#### 3.1.3 退出

- 导航栏提供「退出」按钮，清除会话 Cookie



#### 3.1.4 会话与安全


| 项目      | 规则                                             |
| ------- | ---------------------------------------------- |
| 会话有效期   | 1 小时                                           |
| 会话存储    | httpOnly Cookie（`jiehuanben_session`），JWT 签名   |
| CSRF 防护 | 登录/注册表单携带 CSRF Token，1 小时有效                    |
| 蜜罐字段    | 隐藏 `website` 字段，非空则拒绝                          |
| 速率限制    | 同一用户名 15 分钟内最多 5 次登录/注册失败                      |
| 失效会话处理  | JWT 有效但用户已被删除（如执行 seed 清库）时，自动清除 Cookie 并跳转登录页 |




#### 3.1.5 演示账号

执行 `npm run db:seed` 后可用：


| 用户名        | 密码                  | 说明          |
| ---------- | ------------------- | ----------- |
| `testSeed` | `testSeed-20260811` | 含完整示例相关方与流水 |
| `test`     | `test-20260811`     | 空账本，供测试     |


登录页底部展示演示账号：`testSeed/testSeed-20260811`。

---



### 3.2 概览（首页 `/`）

**用户故事**：作为用户，我希望一眼看到家庭整体借还状况。

**功能点**：

- 展示**总应收**、**总应付**汇总卡片
- 按相关方列出应收明细表（相关方名称、净应收、借入合计、已还合计）
- 按相关方列出应付明细表（结构同上）
- 点击相关方名称跳转至该方详情页

---



### 3.3 记一笔（`/records/new`）

**用户故事**：作为用户，我希望用 guided 流程录入一笔借还，并在提交前确认。

**表单字段**：


| 字段    | 必填  | 说明             |
| ----- | --- | -------------- |
| 账目类型  | ✅   | 应收 / 应付        |
| 交易类型  | ✅   | 借 / 还          |
| 相关方   | ✅   | 下拉选择已有相关方      |
| 金额    | ✅   | 数字输入，实时显示中文大写  |
| 交易日期  | ✅   | 日期选择           |
| 转账方式  | —   | 如「张三微信」        |
| 用途    | —   | 如「装修」          |
| 是否有利息 | —   | 布尔开关           |
| 还款计划  | —   | 一次性 / 分期 / 未指定 |
| 凭证附件  | —   | 可选上传文件         |


**交互流程**：

1. 填写表单
2. 点击「预览」→ 服务端计算提交后余额变化，展示预览结果
3. 点击「确认提交」→ 写入数据库，可选保存附件
4. 成功后跳转流水列表

**约束**：

- 相关方必须预先在「相关方管理」中创建
- 表单页提供快捷入口跳转相关方管理（支持 `returnTo` 回跳）

---



### 3.4 流水列表（`/records`）

**用户故事**：作为用户，我希望筛选、浏览和管理所有借还记录。

**功能点**：

- 表格展示：日期、相关方、账目类型、交易类型、金额、用途等
- 筛选：按相关方、账目类型、年份
- 单条删除
- 批量选择与批量删除
- 点击行跳转详情页
- 操作结果通过 `?flash=` 参数展示 Toast 提示

---



### 3.5 流水详情与编辑



#### 详情（`/records/[id]`）

- 只读展示全部字段
- 展示已上传凭证列表，可点击下载/查看
- 提供「编辑」入口



#### 编辑（`/records/[id]/edit`）

- 修改除 ID 外的所有字段
- 支持删除该条记录

---



### 3.6 图表（`/charts`）

**用户故事**：作为用户，我希望按年度回顾借还趋势。

**功能点**：

- 年度选择器
- **应收柱状图**：各年末累计应收余额
- **应付柱状图**：各年末累计应付余额
- 选定年份后展示该年末（12 月 31 日）各相关方的应收/应付拆分

---



### 3.7 相关方管理（`/parties`）

**用户故事**：作为用户，我希望维护借还对象清单。

**功能点**：

- 列表展示：名称、类型、备注、创建时间
- 新建相关方（弹窗表单）
- 编辑相关方
- 删除相关方（**若存在关联流水则禁止删除**）
- 支持 `?returnTo=` 参数，便于从记一笔页跳转后返回



#### 相关方详情（`/parties/[name]`）

- 该方应收/应付净额卡片
- 借入合计、已还合计
- 完整流水明细表

---



### 3.8 数据管理（`/settings/data`）

**用户故事**：作为用户，我希望备份、恢复和分析账本数据。

#### Excel 导出

- 点击下载，生成含两个工作表的 `.xlsx` 文件：
  - **相关方**：名称、类型、备注
  - **借还流水**：账目类型、交易类型、相关方、金额、日期、转账方式、用途等
- 服务端同步备份至 `data/exports/`（若文件被占用则跳过备份，不影响下载）



#### Excel 导入

两阶段流程：

1. **分析阶段**：上传 `.xlsx` / `.xls`，解析并与数据库比对
2. **确认阶段**：展示 diff 结果，用户确认后仅导入标记为「新增」的行

**去重规则**（以下字段组合相同视为重复）：

> 账目类型 + 相关方 + 交易类型 + 金额 + 日期 + 转账方式 + 用途

**导入行为**：

- 流水中的相关方若不存在，自动创建
- 仅导入 diff 状态为 `new` 的记录



#### PDF 分析报告

- 点击下载 PDF，内容包括：
  - 概览：记录数、相关方数、总应收/应付、净头寸、日期范围
  - Top 应收/应付相关方
  - 各相关方状态：已结清 / 有借无还 / 部分回款 / 持续负债
  - 按年度借还汇总
  - 金额最大的 5 笔交易
  - 规则引擎生成的文字洞察（非 AI 生成）
- 服务端同步备份至 `data/exports/`

---



### 3.9 凭证附件

- 记一笔时可上传文件作为凭证
- 存储路径：`uploads/{recordId}/{filename}`
- 通过 `/api/files/uploads/...` 访问（须登录）
- 详情页展示附件列表

---



## 4. 页面与导航


| 路由                   | 导航名称 | 访问权限 |
| -------------------- | ---- | ---- |
| `/login`             | —    | 公开   |
| `/register`          | —    | 公开   |
| `/`                  | 概览   | 须登录  |
| `/records/new`       | 记一笔  | 须登录  |
| `/records`           | 流水   | 须登录  |
| `/records/[id]`      | —    | 须登录  |
| `/records/[id]/edit` | —    | 须登录  |
| `/charts`            | 图表   | 须登录  |
| `/parties`           | 相关方  | 须登录  |
| `/parties/[name]`    | —    | 须登录  |
| `/settings/data`     | 数据管理 | 须登录  |


未登录访问受保护页面 → 重定向至 `/login?next=原路径`  
未登录访问 API → 返回 401 JSON

---



## 5. 非功能需求



### 5.1 数据隔离

- 每位用户仅可读写自己的相关方与流水
- 数据库层通过 `userId` 外键约束；查询层统一过滤



### 5.2 性能（MVP 预期）

- 面向单用户、数百至数千条记录规模
- 页面以服务端渲染为主，无复杂前端状态管理



### 5.3 兼容性

- 现代浏览器（Chrome、Edge、Firefox、Safari 近期版本）
- Node.js 20+、npm 10+



### 5.4 日志

- 认证、访问、API 错误等事件输出结构化控制台日志
- 时间戳使用北京时间（Asia/Shanghai）



### 5.5 国际化

- 当前仅中文界面，无 i18n 框架

---



## 6. 明确不在 MVP 范围


| 功能          | 说明                       |
| ----------- | ------------------------ |
| AI 智能分析     | 应用内无 LLM 集成；PDF 洞察为规则引擎  |
| 多用户共享账本     | 无家庭组、邀请、权限角色             |
| 云端同步        | SQLite 本地文件，无远程备份        |
| 移动端原生 App   | 仅响应式 Web                 |
| 利息自动计息      | `hasInterest` 仅作标记，不参与计算 |
| 分期还款计划跟踪    | `repaymentPlan` 仅作标记     |
| 邮件/短信提醒     | 无通知系统                    |
| OAuth 第三方登录 | 仅用户名密码                   |


---



## 7. 验收标准（摘要）


| 模块    | 验收条件                                          |
| ----- | --------------------------------------------- |
| 认证    | 注册/登录/退出正常；未登录无法访问业务页；CSRF 无效时拒绝；注册页实时提示用户名占用 |
| 记一笔   | 预览余额正确；确认后数据可查；附件可下载                          |
| 流水    | 筛选、单删、批删生效                                    |
| 相关方   | CRUD 正常；有关联流水时删除被阻止                           |
| 概览/图表 | 数字与手工核算一致                                     |
| Excel | 导出后可再导入；重复记录不被重复写入                            |
| PDF   | 可下载且内容与当前账本一致                                 |
| 隔离    | 用户 A 无法看到用户 B 的数据                             |


自动化测试覆盖：`ledger`、`excel`、`csrf`、`proxy`、`username-availability` 等核心模块（`npm run test`）。

---



## 8. 术语表


| 术语   | 定义                       |
| ---- | ------------------------ |
| 相关方  | 借还资金往来的对象（人 or 机构）       |
| 应收   | 别人欠我的款项累计                |
| 应付   | 我欠别人的款项累计                |
| 流水   | 单笔借或还的记录                 |
| 净头寸  | 总应收 − 总应付                |
| diff | Excel 导入时与数据库比对的结果       |
| 防抖   | 用户停止输入一小段时间后再触发检查，减少无效请求 |


